/**
 * Slice metrics for the 'panel_wood' atlas frame (806x595 source pixels):
 * a fixed-width beveled wood border around a stretchable middle. Re-measure
 * if the atlas is regenerated with a differently-proportioned crop (see
 * art-source/ui/README.md).
 */
export const PANEL_SLICE = { left: 45, right: 45, top: 40, bottom: 40 };
